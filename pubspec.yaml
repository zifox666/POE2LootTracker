name: poe2_loot_tracker
description: "POE2 desktop loot, map, kill, and market tracker."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# THE SINGLE SOURCE OF TRUTH FOR THE PROJECT VERSION.
#
# MAJOR.MINOR.PATCH, optionally followed by +BUILD. Everything else that carries a version is
# generated from this line by `dart run tool/version.dart`:
#
#   * lib/app_version.dart          - compiled into the shell, so the running app knows its own
#                                     version and can compare itself against the newest release.
#   * tracker_host/TrackerHost.csproj - the .NET tracking host is a separate assembly.
#   * the Windows .exe version resource - Flutter passes this line to the runner's CMake itself.
#
# Releasing is: bump this line, run the tool, commit, then push a tag matching its semantic half
# (`1.2.3+45` releases as `v1.2.3`). .github/workflows/release.yml builds the bundle, so do not
# build and upload one by hand. The build number after `+` is local build identity only; it is not
# part of the tag, because a tag that moved underneath a published artifact would invalidate the
# updater's SHA-256 check.
version: 1.1.0+13
environment:
  sdk: ^3.11.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  crypto: 3.0.7
  flutter_localizations:
    sdk: flutter
  desktop_multi_window: 0.3.1
  flutter_svg: 2.3.0
  forui: 0.21.3
  hotkey_manager: 0.2.3
  intl: any
  tray_manager: 0.5.3
  window_manager: 0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: false
  assets:
    - assets/icons/
    - assets/fonts/OFL-Inter.txt
    - assets/fonts/OFL-IBMPlexSans.txt
    - assets/fonts/OFL-SourceHanSans.txt
  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter-Variable.ttf
    - family: IBM Plex Sans
      fonts:
        - asset: assets/fonts/IBMPlexSans-Variable.ttf
    # 思源黑体 (Source Han Sans, the same typeface Google ships as Noto Sans CJK), bundled so the
    # UI never depends on a system CJK font being installed. CN is the Simplified Chinese subset.
    # Two static weights rather than one variable file, so the w600 the UI leans on renders as a
    # real bold instead of a synthesised one.
    - family: Source Han Sans SC
      fonts:
        - asset: assets/fonts/SourceHanSansCN-Regular.otf
        - asset: assets/fonts/SourceHanSansCN-Bold.otf
          weight: 700

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
